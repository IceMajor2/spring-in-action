spring:
  datasource:
    generate-unique-name: false
    name: tacocloud
    url: jdbc:h2:mem:tacocloud
  data:
    rest:
      base-path: /data-api
# custom 'security.oauth2.enable' property that determines creation of `Oauth2SecurityConfig` component
security:
  oauth2:
    enable: false
#logging:
#  level:
#    org:
#      springframework:
#        security: TRACE
